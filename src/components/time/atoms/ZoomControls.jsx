import React from "react";

/**
 * ZoomControls: le etichette di zoom preimpostate ("20 years", "2 years", ecc.)
 * sono state rimosse per mantenere la timeline pulita nel regime giornaliero (24h).
 */
const TimelineZoomControls = () => null;

export default TimelineZoomControls;
